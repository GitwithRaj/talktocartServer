{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Standalone JSON server for TalkToCart",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
