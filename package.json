{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Standalone JSON server for TalkToCart",
  "main": "server.js",
  "scripts": {
    "start": "npx json-server --watch db.json --port 10000 --static false"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
